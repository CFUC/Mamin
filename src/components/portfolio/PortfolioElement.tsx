import styles from "./PortfolioElement.module.css";

const PortfolioElement = ({ image }: { image: string }) => {
  return <div className={styles.container}></div>;
};

export default PortfolioElement;
